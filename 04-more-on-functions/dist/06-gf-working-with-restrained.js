"use strict";
/* function minmumLength<Type extends { length: number }>(
    obj: Type,
    minmum: number
): Type {
    if (obj.length >= minmum) {
        return obj
    } else {
        return { length: minmum }
    }
}

const arr = minmumLength([1, 2, 3], 6)
console.log(arr.slice(0)); */
